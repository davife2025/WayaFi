{
  "name": "@irofi/types",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --out-dir dist --no-splitting",
    "lint": "echo lint"
  },
  "dependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  }
}
